version: '3.8'

services:
  organigramme:
    image: nginx:alpine
    container_name: organigramme-app
    ports:
      - "8080:80"
    volumes:
      - ./src:/usr/share/nginx/html
    restart: unless-stopped
